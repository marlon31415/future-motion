# @package _global_

defaults:
  - _self_
  - trainer: nuplan
  - model: ac_scene_motion
  - datamodule: h5_nuplan_mini
  - callbacks: wandb
  - loggers: wandb
  - resume: empty

hydra:
  run:
    dir: logs/${now:%Y-%m-%d}/${now:%H-%M-%S}

work_dir: ${hydra:runtime.cwd}
seed: 2024
action: fit # fit, validate, test
